# JOURNAL — job-search-zone-a

Une ligne par session. Format : `{YYYY-MM-DD HH:MM} — {objectif} — {fait} — {reste}`. Enrichi par `/handoff`.

- 2026-05-30 — Init projet via /project-kickoff.
- 2026-05-31 10:00 — Pipeline Zone A complet — Livrables 1-10 : JobOffer+Source, FranceTravailSource (OAuth2, multi-keywords), dédup SQLite, ChromaDB embedding, scoring LLM llama3.1:8b (5 critères, parsing défensif), digest, run.py orchestrateur, verdict CLI, view CLI, description stockée en DB — reste : tuning prompt location_fit, cron optionnel, Zone B (FastAPI+Nuxt)
- 2026-06-01 — Chantier UI complet (L0→L9) — Migration seen, FastAPI (GET /offers, GET /offers/{id}, PUT/DELETE verdict), Nuxt 4 + Tailwind + Pinia v3, tableau trié/coloré, 4 vues rapides, filtres manuels, panel détail (critères + verdicts réactifs + zone V2 vide) — reste : boucle d'itération V2 (sémantique favori vs suivi candidature, tuning prompt orchestrateur)
- 2026-06-01 — Migration physique orchestrator/ — git mv job_search/ → orchestrator/job_search/, 19 imports absolus corrigés (job_search.x → orchestrator.job_search.x), orchestrator/__init__.py créé, run validé end-to-end (39 offres scorées, digest produit) — reste : garde-fou Ollama (ping avant run + abort propre), mécanisme rescore des offres parse_failed sans re-fetch API
- 2026-06-01 — Mise à jour docs post-migration — CLAUDE.md (titre, archi 3 briques), stack.md (FastAPI + Nuxt 4 livrés, SQLite partagé), README (commandes réelles orchestrator/ + api/ + web/), STATE.md — reste : aucun (repo nettoyé)
- 2026-06-02 — Refonte scoring double-axe complète (L1→L13) — extractor.py (faits LLM/offre), desirability.py + attainability.py (Python pur), migration SQLite (score→désir+atteign+faits), rescore.py, cockpit "Désir/Reach", digest cadran principal — reste : enrichir profiles/gregoire.yaml (plus de technos, calibration niveaux), observer AI/RAG sur nouvelles offres
- 2026-06-02 — Chantier 1 refacto scoring complet (L1→L9) — filtres durs contract/location (scoring/filters.py, migration filtered_out+filter_reason), désirabilité domain-only graduée (_DOMAIN_GRADIENT 7 valeurs), alias technos (_TECH_ALIASES postgres→sql etc.), rescore --force 68 offres sans LLM, saturation cassée (1×100, reste 0-50) — reste : analyser classement sur nouvelles offres, puis chantier 2 (blocs pondérés)
- 2026-06-03 — Rituel de calibration complet (C-1→C-4, L1→L12) — table human_reviews (migration idempotente), helpers upsert/get, normaliseur criteria API (_build_criteria), bloc calibration cockpit (critères IA lecture seule + champs avis + audit global), endpoints PUT/GET /offers/{id}/review (snapshot figé ai_snapshot_json), disagreement() pur par axe, endpoint /export/calibration (markdown trié désaccord décroissant, lots de 15), bouton export cockpit — reste : tester sur offres réelles, puis chantier 2 profil (blocs pondérés)
- 2026-06-03 — Chantier 2 profil + catégorisation complet (L1→L16+L18)
- 2026-06-09 — Chantier traces LLM complet (L0→L5)
- 2026-06-10 — Lecture traces + outillage jq — ouverture data/traces/extract_facts.jsonl, commande jq pour pretty-print/filtrage — reste : lire les traces à la triple grille en chat — Phase 0 lecture extract_facts (chemin réel orchestrator/job_search/scoring/extractor.py, signature JobOffer pas text, 2 return, fallback distinguable, description non tronquée, second appelant rescore.py), LLMTrace Pydantic 9 champs (scoring/tracing.py), _write_trace non bloquant, instrumentation extractor.py (raw pré-parsing, 2 points de sortie), replay_traces.py racine seed-fixe, 20 traces JSONL peuplées — reste : lire les traces à la triple grille (réappropriation/Wyss/Husain) en chat, puis corriger défauts repérés (fallback silencieux, [:1500], _FEW_SHOT injecté avant description) dans une autre session — profil v2 (level/desire/role_ceiling), extraction enrichie (importance/techno + role_level, ré-extraction 60 offres), atteignabilité refondue (attain_tech pondéré par importance + attain_role portail gradué, min non-compensatoire, blocked_by), désirabilité modulée par envie-techno, catégorisation 4 cases (parfait/reve/atteignable/hors + score_in_category), migration SQLite, contrat criteria_json préservé, rescore 60 offres 0 LLM, cockpit adapté. Post-livrable : score atteignabilité visible (badge + barre), filtre par catégorie, tri a_traiter par catégorie (parfait→rêve→atteignable→hors). L17 tests différé — reste : chantier 3 (piles colorées, UX candidature), tests L17
- 2026-06-10 — Chantier traces viewer complet (L1→L11) — api/traces_reader.py (lecture défensive JSONL), api/traces.py (GET /traces + PUT note), schemas TraceParsedFacts/TraceOut/TraceNoteIn, table trace_notes (migration idempotente), page /traces Nuxt (cartes groupées, 1/3 note auto-save / 2/3 trace monospace), lien header, retrait badge parse_failed OfferDetail.vue — reste : lancer l'error analysis des 23 traces dans /traces, puis correction défauts extractor (few-shot mid-prompt, [:1500], fallback silencieux) en session dédiée
- 2026-06-12 — Chantier hors-périmètre complet (L0→L8) — Phase 0 lecture confirmée (mgmt_role = promotion état existant, chantier S), HorsPerimetreReason enum + derive_hors_perimetre (scoring/hors_perimetre.py), gate continue dans rescore.py avant d/a/s, _build_criteria(None,None)→[] implicite confirmé, migration colonne hors_perimetre_reason (db.py), save_offer branché, statuts verdict hors_perimetre_ok/faux_pos (schemas.py), filtre API hors_perimetre bool, onglet cockpit "Hors-périmètre" (badge reason, bannière détail, verdicts ok/faux-pos), VerdictBadge étendu, vue "À traiter" exclut HP. 9 offres gatées (5 no_tech, 4 mgmt_role), 0 LLM, 5 décisions tracées — reste : inspecter les 5 no_tech (faux positifs extraction ?), correction défauts extracteur, chantier 3 (piles colorées, UX candidature)
- 2026-06-15 — Chantier review humaine complet (L1→L12) — Phase 0 cartographie (category dépend d/a, scoring gardé en interne), Phase 1 migration 4 colonnes review (offers), Phase 2 endpoint PUT category-review + finale dérivée + etat_review, Phase 3 drop 8 colonnes scoring SQLite + retrait code scoring backend, Phase 4 facts+badges techs colorés par importance, Phase 5 geste review 1 clic + indicateur 3 états, Phase 6 validation terrain 5 offres — reste : utilisation terrain
- 2026-06-16 14:42 — Adapter Remotive + multi-source (mode augment) — RemotiveSource(Source) créé (sources/remotive.py, API publique software-dev), run.py multi-source (boucle N sources, flag --no-remotive), smoke test OK (FT 26 offres + Remotive 31 offres, dédup cross-source OK, 31 nouvelles ingérées+scorées) — reste : utilisation terrain (accumuler reviews sur offres Remotive), observer parse_failed sur descriptions HTML longues
- 2026-06-17 — Chantier bouton trace (L1→L5) — Phase 1 back : source_id exposé dans OfferDetail + endpoint GET /traces/counts (134 offres, lecture défensive JSONL). Phase 2 front : store traceCounts + hasTraces(), bouton "Voir la trace" meta bar (actif NuxtLink / grisé span+tooltip), ancrage traces.vue (id par groupe + onMounted expand+scroll via nextTick). Validé terrain — reste : commit, hors-scope reporté (badge N traces, lien retour trace→offre)
- 2026-06-17 — Chantier HTML→Markdown complet (L0→L9) — Phase 0 lecture (bruit isolé à description, chantier S, 141 offres backfillables). Phase 1 html_to_markdown (sources/_clean.py, html2text, garde texte-plat idempotent, 16 tests). Phase 2 schéma+adapters (description_raw ajouté JobOffer, Remotive=MD+raw, FT=raw natif, fetch live vérifié). Phase 3 migration+backfill (colonne description_raw idempotente, save_offer branché, backfill 141 offres 0 LLM). Phase 4 rendu front (marked + @tailwindcss/typography, v-html prose dans OfferDetail.vue, build OK). Validé terrain — reste : commit, re-extraction LLM sur descriptions propres (chantier séparé, hors-scope)
- 2026-06-18 — Chantier canonicalisation techs complet (L0→L8) — Phase 0 relecture (2 consommateurs confirmés : attainability+desirability, articulation profil tranchée). Phase 1 alias.yaml déposé profiles/, loader+canonicalize() dans scoring/aliases.py, 62 tests (3 cas + convergence + non-régression). Phase 2 branchement deux côtés (attainability+desirability reroutés, _TECH_ALIASES+_canonical() supprimés, run.py+rescore.py branchés). Phase 3 rapport unmatched (Counter dans rescore, data/unmatched_techs.txt gitignoré). Phase 4 rescore --force (108 offres, 0 LLM, 228 techs inconnues rapportées). Handoff — reste : chantier divergence front (OfferDetail.vue+index.vue matching brut sans canonicalisation → badges faux), enrichissement alias.yaml depuis rapport unmatched
- 2026-06-25 — Chantier divergence front complet (L0→L7) — Phase 0 relecture ciblée (criteria_json mort → Forme B 2 colonnes, techs_matched/missing jetés → fil à câbler, index.vue pas de matching, profil chargé côté front uniquement pour matching). Phase 1 migration idempotente techs_matched_json+techs_missing_json (db.py), save_offer branché (offers.py), run.py+rescore.py câblés. Phase 2 API read-only (schemas.py OfferDetail enrichi, offers.py désérialise JSON, grep confirmé 0 appel scoring/profil/alias). Phase 3 retrait front (OfferDetail.vue computed listent depuis back, profileSkills+fetchProfileSkills supprimés du store, index.vue appel retiré). Phase 4 rescore --force 108 offres, validation terrain 5 variantes, API TestClient OK. 3 décisions tracées — reste : observer cohérence cockpit↔scoring en usage, enrichissement alias.yaml depuis rapport unmatched
- 2026-06-26 10:30 — Chantier source Indeed complet (L2→L9) — IndeedFileSource adapter fichier (sources/indeed_file.py, lit JSONL, mappe JobOffer, html_to_markdown, lignes malformées skippées), intégration run.py (source branchée, flag --no-indeed), rescore vérifié source-agnostic (0 changement), commande /ingest-indeed (.claude/commands/ingest-indeed.md, orchestration MCP documentée), data/indeed_inbox/.gitkeep créé, validation sur 60 offres réelles (2 JSONL déposés par l'utilisateur via MCP Indeed). Décision gravée : chaîne MCP deux temps — reste : observer ingestion Indeed en usage, surveiller rate limit MCP Indeed
- 2026-06-26 11:15 — Chantier dédup amont Indeed complet (L0→L2) — fingerprint partagé (sources/fingerprint.py, 0 copie inline, 3 sources + endpoint importent), endpoint POST /offers/check-known (api/offers.py, read-only, retourne new_indices), skill /ingest-indeed v2 (filtrage dédup amont + throttle 2-3s inconditionnel + fallback gracieux si API down). Testé : 5 offres connues filtrées, 2 inconnues retournées — reste : test terrain complet via /ingest-indeed avec connecteur Indeed
- 2026-06-30 — Chantier vue candidat complet (L0a→L12) — Phase 0 route /operateur (clone existant, filet sécurité). Phase 1 renommage favori→retenu (schema+VerdictBadge+OfferDetail+FiltersPanel+store+verdict.py orchestrator), migration seen→seen_candidat (colonne SQLite+API+store+composants+orchestrator db.py/offers.py, migration incrémentale auto), tri composite backend (sort=col1,col2&order=dir1,dir2). Phase 2 exclude_category backend, 9 presets store (4 candidat CandidateView + 5 opérateur OperatorView), index.vue 4 onglets (Cibles/Gaps/Filet/Retenues, mount cibles, sans FiltersPanel). Phase 3 isolation UI prop mode candidat/operateur (OfferDetail: review masquée+seul verdict retenu, OffersTable: dots masqués), operateur.vue câblé mode=operateur — reste : tester les deux routes en conditions réelles, dette seen_candidat écrit depuis /operateur (chantier opérateur)
- 2026-07-14 14:00 — Remédiation code mort (ChromaDB, purge, view) — L1→L9 : supprimé embedder.py, purge.py, view.py, insert_stub(), data/chroma/, profile_cache.json, chromadb dep ; nettoyé run.py (étapes renumérotées 1→6), rescore.py, dedup.py, CODEMAP.md ; 3 entrées dette dans DECISIONS.md (30km hardcodé, seuils 50/40, chantier API) — reste : rien (chantier clos)
- 2026-07-13 — Malus séniorité dans atteignabilité — seniority_ceiling ajouté au profil (Profile + gregoire.yaml:intermediate), _compute_seniority_malus() dans attainability.py (−20/cran, cap à senior pour éviter double peine lead), champ seniority_malus dans Attainability, ligne breakdown dans api/offers.py, rescore --force 299 offres 0 LLM. 88 offres impactées (toutes senior/lead, −20 uniforme), 3 ex-parfait → rêve — reste : observer en usage, vérifier breakdown dans le front
